# Giscus (public, safe to commit as example)
# Narrow's Comments.astro reads siteConfig.comments.giscus.* directly (not env);
# these values are a filling guide — put the real ids into src/config/site.ts.
GISCUS_REPO=Lancernix/note
GISCUS_REPO_ID=YOUR_REPO_ID
GISCUS_CATEGORY_ID=YOUR_CATEGORY_ID
